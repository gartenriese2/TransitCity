<UserControl x:Class="TransitCity.City.JobControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:city="clr-namespace:TransitCity.City"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=city:JobViewModel, IsDesignTimeCreatable=True}"
             Width="{Binding Size}" Height="{Binding Size}"
             MouseDown="JobControl_OnMouseDown"
             Visibility="{Binding IsVisible}"
             ToolTip="{Binding Model.NumJobs}">
    <Ellipse Fill="Blue"/>
</UserControl>
