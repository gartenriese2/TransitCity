<UserControl x:Class="TransitCity.City.ResidentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:city="clr-namespace:TransitCity.City"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=city:ResidentViewModel, IsDesignTimeCreatable=True}"
             Width="{Binding Size}" Height="{Binding Size}"
             MouseDown="ResidentControl_OnMouseDown"
             Visibility="{Binding IsVisible}"
             ToolTip="{Binding Model.NumResidents}">
    <Ellipse Fill="DarkGreen"/>
</UserControl>
