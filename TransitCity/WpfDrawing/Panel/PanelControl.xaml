<UserControl x:Class="WpfDrawing.Panel.PanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converter="clr-namespace:WpfDrawing.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:ElementToXConverter x:Key="ElementToXConverter"/>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding Elements, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Left" Value="{Binding RelativeSource={RelativeSource Self}, Converter={StaticResource ElementToXConverter}}"/>
                <Setter Property="Canvas.Top" Value="{Binding RelativeSource={RelativeSource Self}, Converter={StaticResource ElementToXConverter}}"/>
            </Style>
        </ItemsControl.ItemContainerStyle>
        <!--<ItemsControl.ItemTemplate>
            <DataTemplate>
                <Rectangle Width="{Binding Width}"
                           Height="{Binding Height}"
                           Fill="{Binding Fill}"/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>-->
    </ItemsControl>
</UserControl>
